import React from 'react'

const WorkoutTitle = ({title}) => {
  return (
    <span className="font-bold text-2xl text-sky-700 capitalize">
        {title}
      </span>
  )
}

export default WorkoutTitle